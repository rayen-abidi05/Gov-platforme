import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./app/**/*.{ts,tsx}",
    "./components/**/*.{ts,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        olive: {
          950: "rgb(var(--olive-950) / <alpha-value>)",
          800: "rgb(var(--olive-800) / <alpha-value>)",
          600: "rgb(var(--olive-600) / <alpha-value>)",
          400: "rgb(var(--olive-400) / <alpha-value>)",
        },
        gold: {
          500: "rgb(var(--gold-500) / <alpha-value>)",
          300: "rgb(var(--gold-300) / <alpha-value>)",
        },
        cream: {
          50: "rgb(var(--cream-50) / <alpha-value>)",
        },
        ink: {
          900: "rgb(var(--ink-900) / <alpha-value>)",
        },
      },
      fontFamily: {
        display: ["Fraunces", "ui-serif", "Georgia", "serif"],
        body: ["Inter", "ui-sans-serif", "system-ui", "sans-serif"],
      },
      boxShadow: {
        glass: "0 8px 40px -8px rgb(22 33 15 / 0.35)",
      },
      borderRadius: {
        xl2: "1.25rem",
      },
      keyframes: {
         kenburns: {
    "0%": { transform: "scale(1)" },
    "100%": { transform: "scale(1.08)" },
  },
  fadeUp: {
    "0%": { opacity: "0", transform: "translateY(24px)" },
    "100%": { opacity: "1", transform: "translateY(0)" },
  },
  fadeIn: {
    "0%": { opacity: "0" },
    "100%": { opacity: "1" },
  },
  cardIn: {
    "0%": { opacity: "0", transform: "translateY(32px) scale(0.96)" },
    "100%": { opacity: "1", transform: "translateY(0) scale(1)" },
  },
  cardFloat: {
    "0%, 100%": { transform: "translateY(0)" },
    "50%": { transform: "translateY(-8px)" },
  },
  badgePulse: {
    "0%, 100%": { boxShadow: "0 0 0 0 rgb(var(--gold-400) / 0.4)" },
    "50%": { boxShadow: "0 0 0 6px rgb(var(--gold-400) / 0)" },
  },
          revealWipe: {
          "0%": { clipPath: "inset(0 100% 0 0)" },
          "100%": { clipPath: "inset(0 0% 0 0)" },
        },
        photoSettle: {
          "0%": { transform: "scale(1.12)", filter: "saturate(0.7) brightness(0.85)" },
          "100%": { transform: "scale(1)", filter: "saturate(1) brightness(1)" },
        },
      },
      animation: {
         kenburns: "kenburns 26s ease-in-out infinite alternate",
        "fade-up": "fadeUp 0.9s cubic-bezier(0.16,1,0.3,1) both",
        "fade-in": "fadeIn 1.2s ease-out both",
        "card-in": "cardIn 0.9s cubic-bezier(0.16,1,0.3,1) both",
        "card-float": "cardFloat 6s ease-in-out infinite",
        "badge-pulse": "badgePulse 2.5s ease-in-out infinite",
        "reveal-wipe": "revealWipe 1.4s cubic-bezier(0.65,0,0.35,1) forwards",
        "photo-settle": "photoSettle 1.8s cubic-bezier(0.16,1,0.3,1) forwards",
      },
    },
  },
  plugins: [],
};

export default config;