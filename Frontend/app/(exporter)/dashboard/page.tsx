import {OverviewPage} from "@/components/marhp-exporter/OverviewPage";
// import { mockExportRequests, mockNotifications } from "@/lib/mockData"; // swap for real hooks later

export default function Page() {
  return <OverviewPage />;
}